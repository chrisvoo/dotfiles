syntax on
set tabstop=4		" number of spaces used for a tab character
set shiftwidth=4	" number of spaces used when you indent or unindent a line of code
set number			" see the line numbers
set showmatch		" highlight the matching parentheses, braces, and brackets
